# Root pyproject.toml — dev tooling only, not a publishable package.
# To install all packages for local development:
#   pip install -e packages/pypromice[all] -e packages/pypromice-tx[google] -e packages/pypromice-bufr

[tool.pytest.ini_options]
testpaths = ["tests"]
